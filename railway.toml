[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "sh -c 'uvicorn src.agent_server.main:app --host 0.0.0.0 --port ${PORT:-8000}'"
healthcheckPath = "/live"
healthcheckTimeout = 30
restartPolicyType = "always"
